# Hilbert CLI Configuration

# Backend URL (stdio or http://localhost:8765)
HILBERT_BACKEND=stdio

# Default model to use
HILBERT_MODEL=gpt-4o

# Output directory for reports
HILBERT_OUTPUT_DIR=./outputs

# Theme (default, dark, light, mono)
HILBERT_THEME=default